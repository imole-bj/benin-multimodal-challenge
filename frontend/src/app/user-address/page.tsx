import React from "react";
import UserAddress from "@/components/profile/user-address";

export const metadata = {
	title: "Travgo Next js Template",
};

const index = () => {
	return (
		<>
			<UserAddress />
		</>
	);
};

export default index;
